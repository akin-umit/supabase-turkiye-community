name: Validate

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  compose-and-security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate Docker Compose
        run: docker compose --env-file .env.example config -q
      - name: Check shell syntax
        run: |
          find . -type f -name '*.sh' -print0 | xargs -0 -n1 bash -n
      - name: Reject private deployment artifacts
        run: |
          if grep -RInE '(aqenta\.com\.tr|supabase-v2|707539dd|e31f0009|dppkCbzp|Z5Ti_vV)' \
            --exclude-dir=.git --exclude=validate.yml .; then
            echo 'Private deployment identifier or known credential fragment detected.'
            exit 1
          fi

          forbidden_files="$(git ls-files | grep -E '(^|/)(\.env($|\.)|private/|backups/|.*\.(dump|sql\.gz|tar\.gz)$)' \
            | grep -v '^\.env\.example$' || true)"
          if [ -n "$forbidden_files" ]; then
            echo 'Private environment or backup files must not be tracked:'
            echo "$forbidden_files"
            exit 1
          fi
